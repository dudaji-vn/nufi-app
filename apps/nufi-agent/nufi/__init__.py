"""NuFi-owned code that ships inside the Studio image.

The fork stays byte-identical to upstream; this package is where the parts that
are ours live, so a `git subtree pull` never has to reconcile them.
"""
