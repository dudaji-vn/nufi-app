{
  "compilerOptions": {
    "target": "ES2022",
    "module": "Node16",
    "moduleResolution": "Node16",
    "outDir": "dist",
    "rootDir": "src",
    "declaration": true,
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true
  },
  "include": ["src"],
  "// exclude": "Tests run under `bun test`, which resolves bun:test and bare relative imports itself. They are not part of dist (see package.json files), so tsc has no reason to see them.",
  "exclude": ["src/**/*.test.ts"]
}
