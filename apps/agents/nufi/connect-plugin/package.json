{
  "name": "@nufi/paperclip-connect-plugin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Lets a member hand NUFI Agents their own gateway key, using the session they already have at NUFI chat.",
  "paperclipPlugin": {
    "manifest": "./dist/manifest.js",
    "worker": "./dist/worker.js",
    "ui": "./dist/ui/"
  },
  "scripts": {
    "build": "rm -rf dist && node esbuild.config.mjs",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@types/react": "^19.2.17",
    "esbuild": "^0.24.2",
    "typescript": "^5.7.3"
  }
}
