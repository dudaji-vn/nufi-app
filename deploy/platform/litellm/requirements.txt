# TEST ENVIRONMENT ONLY. Consumed by .github/workflows/platform-ci.yml (and the
# local .venv) so `pytest` can import the guardrail package without pulling the
# whole proxy image. This file is deliberately NOT installed into
# litellm/Dockerfile: the base image already ships all three, at versions that
# every pin below conflicts with (httpx 0.28.1, prometheus-client 0.20.0,
# PyYAML 6.0.3), and httpx is a top-level `==` pin of litellm's own. Installing
# this over the image downgrades httpx underneath the proxy and reports success.
httpx==0.27.2
prometheus-client==0.21.1
PyYAML==6.0.2
